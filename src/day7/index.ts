function fixPackages(packages: string): string {
  // Code here
  let start: number;
  let end: number;
  end = packages.indexOf(")");
  return "";
}

const exampleInput7 = "a(cb)de";

console.log({ exampleInput: exampleInput7 });

console.log({ result: fixPackages(exampleInput7) });
